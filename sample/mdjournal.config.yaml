# mdJournal ルート設定ファイル
# 
# 使用方法:
#   npx mdjournal ./mdjournal.config.yaml
#   npx mdjournal -c ./mdjournal.config.yaml

# 著者名（日報タイトルのデフォルト値）
# author: "山田太郎"

# 各設定ファイルへのパス（このファイルからの相対パス）
projects: ./config/projects.yaml
routines: ./config/routines.yaml

# 日報ディレクトリ
reports: ./reports

# タイムライン設定
timeline:
  hourHeight: 60          # 1時間あたりの高さ（ピクセル）
  maxHours: 36            # 最大表示時間（8:00から最大36時間 = 翌日20:00まで）
  defaultStartHour: 8     # デフォルト開始時刻（スロットが空の場合の表示開始時間）
  defaultEndHour: 20      # デフォルト終了時刻（スロットが空の場合の表示終了時間）
  snapMinutes: 15         # ドラッグ時のスナップ単位（分）

# サーバー設定（オプション）
server:
  port: 3001
  cors: http://localhost:5173

# Slack連携設定（オプション）
# slack:
#   enabled: false
#   # Webhook URL（環境変数 SLACK_WEBHOOK_URL でも設定可能）
#   webhookUrl: ${SLACK_WEBHOOK_URL}
#   # 投稿先チャンネル（Webhook URLで設定されている場合は不要）
#   channel: "#daily_report"
#   # 投稿者名
#   username: "日報"
#   # アイコン
#   iconEmoji: ":memo:"
#   # 投稿するセクション（true: 投稿する / false: 省略する）
#   sections:
#     plan: true
#     result: true
#     todo: true
#     note: true
#   # TODOステータスアイコン（Slack shortcode形式）
#   todoIcons:
#     pending: ":black_square_button:"        # 未着手   - [ ]
#     inProgress: ":arrow_forward:"           # 進行中   - [*]
#     onHold: ":double_vertical_bar:"         # 保留中   - [-]
#     completed: ":white_check_mark:"         # 完了     - [x]
